# 项目清理记录（2026-08-31）

## 结论与边界

本次以工作区现有内容为基线，盘点 30,521 个文件（不含 Git 对象库），分析 117 个前端源码文件、172 个 Java 源码/测试文件，以及路由、Vite 配置、测试脚本、资源、Maven/npm 依赖和本地生成目录。未回退已有功能修复，未修改数据库、上传内容、密钥或部署环境，也未提交 Git。

**本次确认的清理项已全部执行，9 个生成目录均已删除。** 首轮直接删除命令曾被执行环境拦截；用户再次要求执行后，重新核对路径和进程，通过已审阅的白名单脚本完成清理。最后的日志被已退出服务的 `start-backend.bat` 残留命令进程占用，确认其没有服务子进程后关闭该残留进程并删除日志。未终止运行中的业务服务。

实际移除 7 个无引用源码文件、2 个废弃内部类、16 个无调用函数/方法、26 个未用导入、5 项无读取状态，以及 18 个 npm / 2 个 Maven 直接依赖、旧图片和独立临时文件。首轮包括 npm 依赖内容共删除 3,797 个文件、约 **48.99 MiB**；补充执行又删除 6,327 个生成文件、约 **982.19 MiB**。两阶段累计删除 **10,124 个文件、约 1,031.17 MiB**，清单中的待删除项为 0。此处是项目文件统计，不表示整台机器净释放空间；工作区外另保留了基线备份和验证产物。

判断依据包括静态/动态 import、Vue 模板自动组件解析、路由懒加载、Worker URL、源码/测试引用、Spring 注解入口、MyBatis 映射和 SHA-256。静态零引用只是候选条件；外部 HTTP 端点、反射绑定方法和历史设计留档没有据此删除。

## 已删除的源文件与资源

| 位置 | 移除理由与替代路径 |
| --- | --- |
| `blog-web/src/components/ParticleBg.vue` | 没有 import、模板组件使用或入口引用；当前背景效果由现有 effects 组件承担。 |
| `blog-web/src/components/PostCard.vue` | 没有页面引用；保留现用 `PostCardSky.vue`。 |
| `blog-web/src/components/PostCardCompact.vue` | 没有页面引用；其卡片布局已不在现行页面中使用。 |
| `blog-web/src/composables/use3DTilt.js` | 没有调用者；现行倾斜效果使用已注册的 `vTilt` 指令。 |
| `blog-web/src/composables/useMagnetic.js` | 没有调用者；现行磁吸效果使用 `vMagnetic` 指令。 |
| `blog-web/src/composables/useTypewriter.js` | 没有调用者；不影响现有标题组件。 |
| `blog-server/src/main/java/com/blog/dto/SiteConfigDTO.java` | 无使用者或绑定入口；`AdminSiteController.save` 实际接收 Map 并自行校验，未改变校验逻辑。 |
| `blog-web/public/images/wuxia-page-lettering-v1.png` | 源码、文档与脚本均无引用，现用 `WuxiaHeadingLettering.vue` 引用 v2；避免 Vite 每次把旧图复制到 dist。 |
| 根目录 `{const` | 0 字节、无扩展名、无引用的误生成空文件。 |

## 已删除的函数、类和状态

Java 路径以下均相对于 `blog-server/src/main/java/com/blog/`；只删除业务层无调用者的方法，没有删除公开 HTTP 端点。

| 文件 | 删除项 | 理由 |
| --- | --- | --- |
| `service/ArticleImportExportService.java` | `parseDocxParagraphs()`、`ParsedDocx` | 已标记 Deprecated，且无调用；DOCX 导入实际使用 `PandocRunner.docxToMarkdown()`。旧实现会丢失短正文、表格和样式。 |
| 同上 | `preview()`、`ParsePreview` | 无控制器、测试或其他调用者；当前 Markdown 预览由前端 Worker 解析。保留 `parseOne`、`upsertFromBytes` 和 Pandoc 导入。 |
| 同上 | `ParseResult.existingId`、`frontMatterRaw`、`bodyRaw` 及赋值 | 删除旧预览后没有读取者；正文仍写入 `ArticleDTO.content`，解析元数据仍保存于 `parsedFrontMatter`。 |
| `service/ArticleService.java` | `listAllPublished()` | 无调用者，现行列表/归档走已有分页查询与可见性过滤。 |
| `service/CommentService.java` | `md5Like()` | 无调用者，头像与评论功能使用现行处理流程。 |
| `security/RateLimiter.java` | `acquireOrThrowCounter()` | 仅转发给 `acquireOrThrow()`，已无调用者；现行限流与过期清理保留。 |
| `mapper/ArticleMapper.java` | `findSlugById()` 及 SQL | 无 Java 调用或 XML/字符串映射引用。 |
| `mapper/TagRelationMapper.java` | `findByTag()`、`findAllByTagId()` 及 SQL | 两条旧全文查询没有调用者；保留现用 `findArticleIdsByTag()` 与文章服务查询。 |
| `blog-web/src/api/admin.js` | `adminSetFeaturedComment`、`adminDeleteUpload`、`adminReorderTools`、`adminReorderToolCategories` | 无消费方的请求包装；服务端对应能力未删除。 |
| `blog-web/src/api/front.js` | `publicToolBySlug`、`publicHotTools`、`confirmSubscribe` | 无消费方；`publicHotTools` 唯一导入本身未使用。邮件确认仍由后端链接处理。 |
| `blog-web/src/components/effects/LightDust.vue` | `rand` | 无调用，粒子参数由现有确定性计算生成。 |
| `blog-web/src/layouts/AdminLayout.vue` | `isToolsActive` | 模板和逻辑均未读取的计算属性。 |
| `blog-web/src/views/admin/Login.vue` | `router` | 无读取的路由实例注入；登录行为和 `useRoute()` 保留。 |

## 已清理的导入

以下为实际移除的未用导入，不包括被整文件删除的旧组件所包含的导入。没有发现需要保留兼容行为的重复同名导入；没有机械删除 `import *`、样式副作用导入或框架注解。

| 位置 | 移除的导入 |
| --- | --- |
| `blog-server/src/main/java/com/blog/controller/AdminProfileController.java` | `PasswordEncoder` |
| `blog-server/src/main/java/com/blog/dto/ProfileUpdateDTO.java` | `NotBlank` |
| `blog-server/src/main/java/com/blog/dto/ToolReorderDTO.java` | `Min` |
| `blog-server/src/main/java/com/blog/entity/EmailSubscription.java` | `TableField` |
| `blog-server/src/main/java/com/blog/security/SecurityConfig.java` | `AuthenticationManager`、`AuthenticationConfiguration` |
| `blog-server/src/main/java/com/blog/service/ArticleService.java` | `Comment` |
| `blog-server/src/main/java/com/blog/service/CaptchaService.java` | `BizException` |
| `blog-server/src/main/java/com/blog/service/OperationLogService.java` | `Transactional` |
| `blog-server/src/main/java/com/blog/service/ToolService.java` | `ToolDailyClick` |
| `blog-server/src/main/java/com/blog/service/VisitLogService.java` | `List` |
| `blog-server/src/main/java/com/blog/mapper/TagRelationMapper.java` | `Article` |
| `blog-server/src/test/java/com/blog/ReviewFixIntegrationTest.java` | `User` |
| `blog-web/src/main.js` | `ElMessage`、`ElMessageBox`、`ElNotification`；使用处自行导入，共享 CSS 保留 |
| `blog-web/src/components/AppHeader.vue` | `nextTick` |
| `blog-web/src/components/ShareBar.vue` | `ElMessage` |
| `blog-web/src/layouts/AdminLayout.vue` | `Files` |
| `blog-web/src/views/admin/Profile.vue` | `computed` |
| `blog-web/src/views/admin/SubscriptionList.vue` | `computed` |
| `blog-web/src/views/admin/Login.vue` | `useRouter` |
| `blog-web/src/views/front/About.vue` | `watch` |
| `blog-web/src/views/front/Tools.vue` | `watch`、`publicHotTools` |
| `blog-web/scripts/review-harness.js` | `nextTick` |

## 已清理的依赖和配置

`blog-web/package.json` 移除下列 18 个无源码、脚本或构建使用的直接依赖，使用 npm 离线更新锁文件与安装目录，共移除 59 个包。剩余锁定依赖的版本没有变化。

- `@tiptap/extension-color`
- `@tiptap/extension-font-family`
- `@tiptap/extension-highlight`
- `@tiptap/extension-image`
- `@tiptap/extension-link`
- `@tiptap/extension-table`
- `@tiptap/extension-table-cell`
- `@tiptap/extension-table-header`
- `@tiptap/extension-table-row`
- `@tiptap/extension-task-item`
- `@tiptap/extension-task-list`
- `@tiptap/extension-text-style`
- `@tiptap/extension-underline`
- `@tiptap/starter-kit`
- `@tiptap/vue-3`
- `turndown`
- `turndown-plugin-gfm`
- `highlight.js`

现用编辑器是 `md-editor-v3`，其必需依赖继续由 npm 锁文件维护；Markdown 安全、解析和 Worker 相关依赖保留。

`blog-server/pom.xml` 移除无源码或配置功能使用的 `spring-boot-starter-data-redis` 与 `qiniu-java-sdk`。现行缓存使用 Caffeine、JWT 撤销使用数据库、图片上传使用 `LocalStorageService`，没有 Redis/Qiniu 客户端调用。同步移除 `application-example.yml` 中无效的 `spring.data.redis` 示例配置；没有改动被忽略的本地配置。

更新 README、需求文档、`vMagnetic` 与 Vite 注释，避免继续声称已删除的组件/依赖正在使用。`.gitignore` 新增临时文件、Python/Vite 缓存、覆盖率与 Playwright 产物规则，不忽略设计留档。

## 已删除的独立垃圾文件

| 位置 | 理由 |
| --- | --- |
| `blog-web/dist.tgz` | 旧前端部署包，可由正式构建/打包流程重建。 |
| `blog-web/.vite-dev.log` | 已结束的开发服务器日志。 |
| `blog-web/review-fix-build.log` | 历史构建日志，本次验证日志放在工作区外。 |
| `blog-server/cp.txt` | 手工编译的临时 classpath 列表。 |
| `blog-server/libs.txt`、`blog-server/libs_unix.txt` | 手工整理的依赖路径列表，Maven 管理真实依赖。 |
| `blog-server/sources.txt`、`blog-server/sources_list.txt`、`blog-server/sources_unix.txt`、`blog-server/sources_verify.txt` | 手工编译的源码列表，不参与 Maven 构建。 |
| `blog-server/rebuild-jar.py` | Maven 曾损坏时的离线临时重打包脚本，依赖旧 JAR 与本机硬编码路径；正常 Maven 构建已验证。 |
| `blog-server/verify-compile.bat` | 基于旧 `target/lib` 和手工源码列表的临时编译脚本，Maven verify 已替代。 |
| `blog-server/package-lock.json` | Java 项目中的空 npm 锁文件，无 package.json 或 npm 构建入口。 |

## 已删除的生成目录

以下目录及其内容现已全部删除，原本均被 Git 忽略。表格记录清理前的占用。**`target/` 和 `dist/` 已不存在，运行或部署前须按正常流程重新构建。** 此前最终验证使用工作区外的源码副本和新构建目录，避免旧 class 文件掩盖问题；本次仅删除生成内容，不重复构建以免立即重新生成待清理文件。

| 位置 | 约占用 MiB | 移除理由 |
| --- | ---: | --- |
| `out/` | 673.78 | 历史 Edge/CDP 验收浏览器 profile、缓存、截图和 IDE 输出。未发现项目进程占用。 |
| `blog-server/build/` | 151.90 | 历史离线编译、解包的 BOOT-INF 和打包副本。 |
| `blog-server/META-INF/` | 0.01 | Spring Boot JAR 解包元数据，不是 Maven 源资源。 |
| `blog-server/org/` | 0.38 | 解包得到的 Spring Boot loader 类，不是业务源码。 |
| `blog-server/target/` | 105.59 | Maven 编译、测试和打包输出，可以重建。 |
| `blog-web/dist/` | 28.29 | Vite 生产构建输出，可以重建。 |
| `blog-web/node_modules/.vite/` | 22.17 | Vite 预构建缓存，不包括其余已安装的开发依赖。 |
| `blog-web/node_modules/.vite-temp/` | 0.01 | Vite 临时配置打包缓存。 |
| `logs/` | 0.04 | 历史天气联调的 session/start/start-error/build 日志。 |

本次已运行 [清理脚本](../scripts/clean-generated.ps1)。以后在本机 PowerShell、项目根目录可以复用：

```powershell
# 默认仅展示清单，不删除
.\scripts\clean-generated.ps1

# 明确预演删除过程
.\scripts\clean-generated.ps1 -Apply -WhatIf

# 关闭项目服务和相关 QA 浏览器后执行
.\scripts\clean-generated.ps1 -Apply
```

脚本只处理上述白名单，验证路径位于仓库内，拒绝目录链接/重解析点，检测正在运行的项目进程；没有 `git clean` 或针对整个 node_modules 的删除。已执行 `-Apply -WhatIf` 验证及实际 `-Apply` 清理，并再次检查 9 个目标目录全部不存在。需要运行项目时再按正常流程重新构建。

## 明确保留的项目

| 位置/类型 | 保留理由 |
| --- | --- |
| Spring `@Configuration`/`@Bean`/控制器/异常处理/定时任务/生命周期方法 | 框架在运行期调用，不要求普通 Java 引用。包括 CacheConfig、MybatisPlusConfig、OpenApiConfig、WebConfig 和 SystemController。 |
| `db/migration/V2__Review_security_fixes.java`、迁移 SQL、`db/init.sql` | Flyway 按位置和命名发现迁移；初始化 SQL 仍是部署资料，不能当作无 import 文件删除。 |
| DTO/entity 的 getter/setter、配置属性 setter、Jackson/MyBatis 使用的成员 | 参与绑定、序列化与数据库映射，不能按静态调用计数删除。 |
| 未被当前前端调用的后台/公共 HTTP 端点 | 可能供邮件链接、外部客户端或运维调用；本次仅删除无消费方的前端包装函数。 |
| `blog-web/public/media/wuxia-swordsman-intro.mp4`、`public/images/wuxia-swordsman-poster.webp` | 当前页面不使用，但设计记录明确保留的用户原视频衍生素材；不擅自删除。 |
| `blog-web/public/fonts/*` 及许可/NOTICE | 现用字体和再分发许可；许可即使没有代码引用也必须保留。 |
| `blog-web/design-qa-assets/`、`design-prototypes/`、设计/需求/评审文档 | 测试脚本、设计来源、验收基线、阶段记录，不属于运行缓存。 |
| `.idea/`、`.vscode/`、`.claude/`、`.workbuddy/`、`.mimosa/`、`.zcode/` | 用户编辑器/工具状态，不能仅凭“被 Git 忽略”认定可丢弃。 |
| `release-sanitize/` | 旧隐私整理留档，不能确认可从其他文件恢复，保留。 |
| `ip2region.xdb`、后端字体、DOCX 模板 | 按配置/运行时路径加载，访问统计与文章导出可能需要。 |
| `uploads/`（如存在）、`server-config/`、本地 application/env 文件、deploy 脚本 | 用户数据、实际部署配置或凭据，本次未清理。 |
| 剩余 `node_modules/` | 当前开发与验证依赖，保留可直接运行环境；只通过 npm 移除已证明无用的包。 |

## 重复图片清单

按 SHA-256 发现 70 组字节完全相同的图片，多数是同一画面在不同验收阶段的 before/after 基线，另有正式标题图片与设计预览副本。**全部保留**：删除会破坏阶段证据、文档链接或可重跑的截图输出结构；运行时重复与设计留档重复不能等同处理。

逐组路径、哈希和字节数见 [重复资源明细](CLEANUP-DUPLICATES-2026-08-31.json)。没有对非相同内容图片进行“看起来相似”的自动删除。

## 验证

| 检查 | 结果 |
| --- | --- |
| 清理前前端生产构建、后端 JDK 17 verify | 均成功，后端 42 项测试通过。 |
| 清理后前端生产构建 | 成功，输出到工作区外，未复用旧 dist。 |
| `npm run test:theme-layout` | sunny / ink 主题静态约束通过；本次没有指定 Git 历史布局基线（相比文件数为 0），不把它描述为像素对比测试。 |
| `npm run test:review` | 通过：Markdown XSS、YAML Worker、关闭评论、回复分页、编辑器路由复用、未保存修改保护、服务端退出。 |
| 天气浏览器回归 | 11 个场景通过，包括 IP 定位与缓存、区县展示、移动端溢出、备用来源、网络故障/重试、慢响应竞争、损坏/禁用存储、无效坐标和搜索故障。结果写入工作区外，没有覆盖原验收截图。 |
| 清理后后端完整 verify | 用最终源码的独立副本、JDK 17 和隔离 H2 从空输出目录构建；42 项测试通过，0 失败、0 错误、0 跳过，打包成功。移除 Redis/Qiniu 后再次完整通过。 |
| 最终 JAR 检查 | 不再包含 SiteConfigDTO、ParsedDocx、ParsePreview，以及已移除的 Redis Starter / Qiniu SDK；验证副本源码和 pom 与工作区最终内容逐字节一致。 |
| 依赖锁文件核对 | npm 共删除 59 个包，剩余包的锁定版本没有变化。 |
| 二次引用扫描 | 前端入口不可达文件、未用导入/顶层导出候选为 0；Java 明确未用导入和未调用私有方法候选为 0。扫描不构成对任意运行时反射/数据库内容的穷尽证明。 |
| 清理脚本 | PowerShell 语法解析、`-Apply -WhatIf` 与实际 `-Apply` 均已完成；确认 9 个目标目录全部不存在，源码、剩余依赖、本地配置和设计留档仍在。 |
| `git diff --check` | 通过。 |

可复现验证入口：前端 `npm run build`、`npm run test:theme-layout`、`npm run test:review`；后端使用 JDK 17 执行 `mvn -B -Dspring.config.name=application-example verify`。浏览器测试使用本机 Edge 无头模式与模拟 API，不访问生产服务。H2 测试不能代替真实 MySQL、SMTP、外部天气服务和 Pandoc 的部署验收。
